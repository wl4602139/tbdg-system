# 2026-08-31 工作日志：能耗能效交互联动、台账精简与对标管理弹窗优化及全量线上发布

## 📋 工作背景与任务概述
今日根据业务与产品体验评审要求，对「能耗能效分析」模块（单位产品能耗、单位产值能耗、对标管理）与「集中监管」模块（碳排放监测）进行了多维度的细节交互修复、表格台账精简与工业级 UI 视觉提升，并完成生产环境（阿里云 ECS `8.215.89.194:3000`）的全量部署。

---

## 🛠️ 详细完成工作事项

### 1. 组织拓扑树底栏与碳排放监测页面精简
- **组织拓扑树**：在 `StandardOrgTree` 与 `OrgTreeSidebar` 中彻底移除底部统计栏（如 `6大一级单位 · 30二级单位 100% 在线`、`15大零碳园区 · 全域微网 100% 在线`），使树节点展示区自适应铺满高度。
- **碳排放监测**：
  - 移除了卡片右上角状态标签（`核算终值`、`抵消前基准`、`3 大抵消合规` 等）；
  - 移除了卡片标题英文副标题，统一为纯中文；
  - 移除了图表和明细台账区域标题前附带的企业名称前缀。

### 2. 单位产品能耗（`/zero-carbon/energy/unit-product`）卡片与图表全维联动
- **卡片交互与选中高亮**：
  - 引入 `selectedKpiId` 状态与 `onClick` 事件，支持在 5 大介质卡片间平滑切换；
  - 选中卡片应用蓝色高亮边框（`border-2 border-[#1677ff]`）、柔和渐变背景与微动效；
- **图表数据与 Y 轴动态适配**：
  - 点击综合能耗：展示综合单耗走势，Y 轴为 `tce/万kVA`（变压器）/ `tce/km`（线缆）；
  - 点击电耗：展示电单耗走势，Y 轴为 `kWh/kVA`（变压器）/ `kWh/km`（线缆）；
  - 点击蒸汽消耗：展示变压器干燥工序蒸汽单耗走势，Y 轴为 `t/万kVA`；
  - 点击氮气消耗：展示线缆立塔交联工序氮气单耗走势，Y 轴为 `m³/km`；
  - 点击天然气与水消耗：展示燃气与新鲜水单耗走势；
  - 产业切换自动校准介质（变压器 ⇄ 线缆）。

### 3. 单位产品能耗台账与计量单位工业级规范
- **台账表格列精简**：
  - 移除了 **「统计期总产量」** 列；
  - 移除了 **「所属分类」** 标签列；
  - 移除了 **「同比变动」** 百分比列；
- **单耗计量口径去“台”**：
  - 将变压器单耗全面规范为工业级容量单位：`tce/万kVA`、`kWh/kVA`、`t/万kVA`、`m³/万kVA`，彻底移除“x台”和“/台”。

### 4. 单位产值能耗（`/zero-carbon/energy/unit-output`）标题纯净化
- 移除了页面各区域标题中动态绑定的公司名称前缀：
  - 核心指标卡片：`万元产值能源单耗指标`；
  - 单耗变化趋势：`{activeMetricMeta.name}变化趋势`；
  - 经营/项目公司对比：`各经营单位单位产值能耗` / `各项目公司单位产值能耗`；
  - 产值综合能耗明细：`各经营单位产值综合能耗明细` / `各项目公司产值综合能耗明细`；
  - 历史明细台账：`万元产值能耗历史明细台账`。

### 5. 对标管理（`/zero-carbon/energy/benchmark`）弹窗与卡片优化
- **弹窗放大与空间舒适化**：
  - 将「录入 / 维护能效对标基准与标准值」弹窗宽度从 `max-w-lg` 扩大至 **`max-w-3xl`（约 768px）**；
  - 内边距与留白优化（`p-6`、`space-y-4`），输入框高度与字号更加舒适；
  - 增加顶部渐变阴影质感图标徽章与副标题说明；
- **移除卡片右上角标签**：
  - 彻底移除了三大基准分类卡片右上角的 `6 项工序标杆`、`3 大强制/引导门槛`、`6 项内控考核` 角标。

---

## 🚀 构建与线上发布验证
1. **本地生产构建**：`npm run build` 成功完成，60 个全量静态页面生成完毕（耗时 3.7s）；
2. **远程 ECS 部署**：
   - 打包 `tbea-nengtan-dist.tar.gz` 并通过 SFTP 上传至阿里云 ECS（`8.215.89.194`）；
   - 解压至 `/var/www/tbea-nengtan`，更新目录权限并重载 Nginx 服务；
3. **在线访问地址**：
   - 生产环境：`http://8.215.89.194:3000`
   - 本地开发：`http://localhost:3000`
