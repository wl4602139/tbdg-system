# 特变电工能碳数字化双中心 · 工作日志

**日期**：2026-09-11  
**责任团队**：特变电工能碳数字化研发团队（PM / 前端架构师 / UI-UX / DevOps）  
**关联里程碑**：左上角品牌栏规范 100% 像素级对齐、全站 UI 设计规范多 Agent 审查落地、ECS 生产环境 (8.215.89.194) 双端热重载发布与代码库版本管理  

---

## 一、 今日工作概述

依据特变电工官方最新下发的《UI页面修改 (2).pdf》权威设计规范及用户最新核验指示与截图批注（`media_1789121821197.png` 与 `media_1789122403997.png`），今日重点攻坚并完成了以下 4 项核心任务：
1. **左上角品牌栏与业务中心下拉菜单 100% 像素级对齐设计稿**：
   - 提取官方无底纯白矢量 LOGO（`/logo-white.png`，440×38 RGBA），彻底剥离此前生硬的白色圆角衬底卡片；
   - 品牌区全要素调整为严格水平中轴居中对称（Logo、中文大标题 18px、英文副标题 8px）；
   - 业务中心切换器升级为标准的圆角半透明胶囊（`rounded-xl border border-white/40 bg-white/10`），左侧内嵌纯白 `Globe` 图标，右侧为 `ChevronDown`；
2. **英文副标题缩小字号并强制单行显示（响应用户红框批注）**：
   - 将英文小字字号由 9.5px 精调为 **`8px`**，字母间距调整为标准的 **`tracking-[0.1em]`**；
   - 强制显式注入 **`whitespace-nowrap`** 与 `leading-none`，彻底根除“CENTER”单词意外折行掉到第二行的视觉缺陷；
3. **编写第 37 篇标准化开发手册**：
   - 编制并归档 [`开发手册/37_左上角品牌规范与双中心导航交互开发手册.md`](../开发手册/37_左上角品牌规范与双中心导航交互开发手册.md)；
   - 同步更新 [`开发手册/README.md`](../开发手册/README.md) 索引；
4. **阿里云 ECS 生产环境 (8.215.89.194) 双端同步热重载发布**：
   - 经本地双端 `pnpm build` 全量通过（暗黑端 77/77 路由 0 报错，浅色端 77/77 路由 0 报错）；
   - 通过 SFTP 自动化推送 tarball 资产，原子解压至生产路径 `/var/www/tbea-nengtan/`（暗黑端）与 `/var/www/tbea-nengtan-old/`（浅色端）；
   - 重新赋权并平滑执行 Nginx 热重载（`nginx -t` & `systemctl reload nginx`）；
   - 线上 6 处核心端点实测 HTTP 200 OK 且 100% 包含 `logo-white.png`；
5. **Git 代码库版本管理与推送**：
   - 严格遵循交付纪律，在用户下达明确指令后组织本次成果的代码暂存、规范提交与远端推送。

---

## 二、 核心功能与视觉整改详细清单

### 1. 左上角品牌区域与业务中心选择器
- **组件文件**：
  - 暗黑端：`产品原型/components/shared/platform-shell.tsx`
  - 浅色端：`产品原型-旧/产品原型/components/shared/platform-shell.tsx`
- **重构要点**：
  - **白底实体卡片剥离**：彻底移除 `<div className="bg-white rounded px-1.5 py-0.5">`，换用纯白矢量 SVG/PNG `logo-white.png`；
  - **居中排版对齐**：采用 `flex flex-col items-center text-center px-4 pt-6 pb-5` 单一统一容器，剔除挤在右侧的折叠按钮；
  - **中文大标题**：18px 粗体白字，自适应切换“零碳园区集控中心”与“产品碳足迹集采中心”；
  - **英文副标题**：8px 单行白字 `tracking-[0.1em]`，`whitespace-nowrap`，彻底杜绝换行；
  - **胶囊下拉按钮**：`rounded-xl`，白描边微透底，内嵌纯白 Globe 地球图标与当前中心名称。

### 2. 全局导航与组织架构树垂直居中修复
- **子菜单文字居中**：子菜单由 `block h-[30px] leading-[30px]` 升级为 `flex items-center h-[32px] px-2.5 rounded-md text-xs`；
- **主菜单与拓扑树**：主菜单统一为 `h-[36px]`，拓扑树统一为 `h-[30px]`，Flexbox 交叉轴居中，浅色选中底色统一为 `#EBF3FF` + 蓝字 `#2C7CFF`。

### 3. 重复 Key 报错治理与数据去重
- 修复 `compare-view.tsx` 中“智慧能源”节点重名导致的 React Key 重复报错，通过 `Set` 去重与复合 key `key={`${r.unit}-${i}`}` 彻底消除控制台红字。

---

## 三、 生产环境双端部署验证 (8.215.89.194)

### 3.1 自动化发布执行指标
- **部署脚本**：`scratch/deploy_8215.py`
- **暗黑端包体积**：`tbea-dark.tar.gz` (9.85 MB)
- **浅色端包体积**：`tbea-light.tar.gz` (9.81 MB)
- **宿主机解压路径**：
  - 暗黑科技蓝版 (Port 3000)：`/var/www/tbea-nengtan/`
  - 浅色商务办公版 (Port 3001)：`/var/www/tbea-nengtan-old/`
- **Nginx 状态**：`syntax is ok`, `test is successful`, `reload nginx SUCCESS`

### 3.2 生产公网端点 HTTP 校验结果
1. `http://8.215.89.194:3000/zero-carbon/monitor/indicator` ➔ **HTTP 200 OK**（`has logo-white: True`）
2. `http://8.215.89.194:3001/zero-carbon/monitor/indicator` ➔ **HTTP 200 OK**（`has logo-white: True`）
3. `http://8.215.89.194:3000/zero-carbon/screen` ➔ **HTTP 200 OK**（`has logo-white: True`）
4. `http://8.215.89.194:3001/zero-carbon/screen` ➔ **HTTP 200 OK**（`has logo-white: True`）
5. `http://8.215.89.194:3000/carbon-footprint/cockpit` ➔ **HTTP 200 OK**（`has logo-white: True`）
6. `http://8.215.89.194:3001/carbon-footprint/cockpit` ➔ **HTTP 200 OK**（`has logo-white: True`）

---

## 四、 产出文档与归档资产

1. **开发手册**：[`开发手册/37_左上角品牌规范与双中心导航交互开发手册.md`](../开发手册/37_左上角品牌规范与双中心导航交互开发手册.md)；
2. **总目录索引**：[`开发手册/README.md`](../开发手册/README.md)；
3. **变更全量档案**：[`MODIFICATIONS_LOG.md`](../MODIFICATIONS_LOG.md)（第 6.1 节原位更新）；
4. **工作日志**：[`工作日志/2026-09-11_工作日志_左上角品牌规范全量对齐与双端同构线上部署.md`](./2026-09-11_工作日志_左上角品牌规范全量对齐与双端同构线上部署.md)。
