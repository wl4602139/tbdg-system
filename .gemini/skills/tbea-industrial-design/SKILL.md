---
name: tbea-industrial-design
description: "特变电工能碳数字化双中心专属设计系统与工程规范。固化工业实用主义、极简克制信息密度、空状态无冗余文案、表格44px强制行高、暗黑科技蓝与浅色双端同构同步、设备能源介质自适应与权威工序映射准则。开发、重构或优化本项目任何页面时必须严格遵循。"
---

# 特变电工能碳数字化双中心 · 专属设计系统与工程开发规范 (TBEA Industrial Design System)

本规范深度提炼自用户长期工程交互习惯与特变电工（TBEA）“双中心”平台的工业实战场景。在本项目中进行任何新功能开发、页面重构、UI样式调整或数据对接时，**必须优先激活并全量遵循本 Skill**。

---

## 一、 核心设计哲学：工业实用主义与极致克制 (Industrial Pragmatism & Restraint)

### 1. 极简克制：拒绝冗余信息与过度设计
- **空状态原则**：当某企业、车间或产线无相关数据/无对应工序/无产品时，**只显示一句话核心结论**，严禁生成大段解释文字、依据文件名称、免考核证明或装饰性大图标。
  - **无工序时统一显示**：`暂无相关工序！`
  - **无产品时统一显示**：`暂无相关产品！`
  - **无告警/无设备时**：`暂无相关记录！` 或 `暂无相关设备！`
  - 样式使用居中中性色排版（暗黑端 `text-sm text-muted-foreground`，浅色端 `text-sm text-slate-400`），保持页面整洁利落。
- **去除伪对比，聚焦自身时序**：
  - 光伏、热泵、储能、变压器/线缆单耗等设备或车间指标，**仅与自身历史或自身基准对比**（环比、同比、额定设计值、月度消纳率）；
  - **严禁擅自捏造跨工厂、跨单位的横向对抗对比**。

### 2. 信息信噪比：数字即生产力
- 指标数据卡片必须遵循统一层级：
  1. **标题**：`text-xs font-bold`，右侧配备极简状态或类别徽章（`text-[9.5px] px-1.5`）；
  2. **主数值**：必须加粗并使用等宽字体：`font-mono font-extrabold text-lg`（科技蓝/前景色）；
  3. **单位**：紧跟主数值右侧，字号缩小：`text-xs font-normal text-muted-foreground font-sans`；
  4. **环比/同比栏**：底部以极细边框（`border-t border-border/60`）分隔，展示 `环比 / 同比`，带清晰指示箭头（`↓ / ↑`）与能效语义着色（绿色代表节能下降，红色代表超标上升）。

---

## 二、 布局与表格强制规范 (Layout & Table Standards)

### 1. 表格行高强制固定为 44px
- 全系统所有数据表格（Data Table）的行高必须严格设定为 **`44px`**（`h-[44px]` 或 `leading-[44px]`），单元格垂直居中。
- 适用范围：订单能耗追溯、设备在线台账、碳足迹 LCA 排放清单、工序能效对标表等。
- 理由：`44px` 是高密度工业 ERP 与 SCADA 集中监控的最佳人机工程高度，兼顾触控点选与长列表数据快速扫描。

### 2. 栅格与卡片间距规范
- **整体卡片容器**：`bg-card`（暗黑） / `bg-white`（浅色），圆角统一为 `rounded-xl`，边框 `border border-border`（暗黑） / `border border-slate-200`（浅色），阴影采用精细级 `shadow-xs` 或 `shadow-2xs`。
- **Bento 网格卡片间距**：网格间距统一为 `gap-3` 到 `gap-3.5`，卡片内边距统一为 `p-3.5` 到 `p-4`。

---

## 三、 工业业务与数据映射准则 (Industrial Data & Process Rules)

### 1. 严格依据权威工序对应表 (`生产单位与涉及关键工序对应表(1).et`)
- **零工序企业白名单**（在工序管控板块严禁回滚全量指标，必须直接显示 `暂无相关工序！`）：
  - 沈变公司：`智慧能源`、`印能公司`、`露娜公司 (特变电工露娜智能)`
  - 衡变公司：`南京电研`、`上开`、`柯贝尔`
  - 新变厂：`银利电气`、`智慧能源`
  - 鲁缆公司：`智缆公司`、`昭和公司`、`曙光公司`（特种电缆无关键制造工序）
- **实体工序单位**：必须严格只显示该单位登记拥有的工序指标（例如 `沈变本部` 仅看干燥与试验；`和新套管` 仅看套管干燥；`鲁缆本部` 仅看拉丝与交联），严禁交叉混杂。

### 2. 工业设备与能源介质自适应规则
- **电力为主**：大部分现代工业设备（变压器试验站、拉丝机、SMT贴片机组、真空泵、除湿机）仅使用电力。
- **卡片介质自适应**：设备在线监控卡片必须根据设备接入的真实能源类型（电、水、蒸汽、天然气）动态呈现介质标签（例如明确标注“电力”或“蒸汽”），禁止硬编码全介质或错误介质。
- **管道工作压力规范**：工业管网监测中，原“管道工作压力”一律严格映射并展示为 **“蒸汽消耗量”**（工程单位为 `t` 或 `t/h`）。

---

## 四、 双端同构同步与部署交付流水线 (Dual-Theme & DevOps Workflow)

特变电工数字化平台运行在双端生产镜像环境中，所有代码更改**必须实现 100% 双端同构更新**。

### 1. 双端环境与样式映射规则
- **暗黑科技蓝主环境**：
  - 部署端口：`8.215.89.194:3000`
  - 本地路径：`d:/Project/TJ-nengtan/产品原型`
  - 核心样式：`bg-card`、`bg-panel`、`border-border`、`text-foreground`、`text-muted-foreground`、青蓝霓虹光效
- **浅色办公商务镜像**：
  - 部署端口：`8.215.89.194:3001`
  - 本地路径：`d:/Project/TJ-nengtan/产品原型-旧/产品原型`
  - 核心样式：`bg-white`、`bg-slate-50`、`border-slate-200`、`text-slate-900`、`text-slate-500`、AntD蓝（`#1677ff`）
- **同步守则**：凡修改一端，必须使用脚本或精确替换同步至另一端，严禁遗漏。

### 2. 部署与交付闭环命令模版
在每次任务交付前，必须执行以下完整闭环：
1. **本地全量静态路由编译校验**：
   ```bash
   # 暗黑端
   cd d:/Project/TJ-nengtan/产品原型 && pnpm build
   # 浅色端
   cd d:/Project/TJ-nengtan/产品原型-旧/产品原型 && pnpm build
   ```
   确保双端 76/76 静态路由均在 3 秒内编译成功，0 语法报错。
2. **远程双端原子发布 (`8.215.89.194`)**：
   ```powershell
   powershell -Command "tar -czf $env:TEMP\tbea-dark.tar.gz -C 'D:\Project\TJ-nengtan\产品原型\out' .; scp $env:TEMP\tbea-dark.tar.gz admin@8.215.89.194:/tmp/; ssh admin@8.215.89.194 'sudo rm -rf /var/www/tbea-nengtan/* && sudo tar -xzf /tmp/tbea-dark.tar.gz -C /var/www/tbea-nengtan/'; tar -czf $env:TEMP\tbea-light.tar.gz -C 'D:\Project\TJ-nengtan\产品原型-旧\产品原型\out' .; scp $env:TEMP\tbea-light.tar.gz admin@8.215.89.194:/tmp/; ssh admin@8.215.89.194 'sudo rm -rf /var/www/tbea-nengtan-old/* && sudo tar -xzf /tmp/tbea-light.tar.gz -C /var/www/tbea-nengtan-old/ && sudo systemctl reload nginx'"
   ```
3. **健康检查与 Git 归档**：
   - 使用 PowerShell 访问 `http://8.215.89.194:3000/...` 与 `http://8.215.89.194:3001/...`，确保 HTTP 均为 200；
   - 双端执行 `git add .`、`git commit -m "..."` 并推送至 `origin/main`。

---

## 五、 本 Skill 的维护与持续演化机制 (Maintenance Guidelines)

1. **常态化更新触发点**：
   - 当用户提出新的视觉偏好（如特定色值、字号微调、交互形式）；
   - 当收到新的需求业务文档或规则变更（如新增园区、新核定工序对应表、新计量介质）；
   - 当上线新模块或新内页时。
2. **更新原则**：
   - 不断提炼并沉淀用户直觉认知；
   - 发现“过度设计”苗头时及时追加反模式（Anti-patterns）清单；
   - 保持本文档简练、精要，作为本项目的最高设计与交付宪章。
